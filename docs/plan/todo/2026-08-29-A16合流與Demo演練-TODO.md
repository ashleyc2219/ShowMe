# 2026-08-29 A16 合流與 Demo 演練 TODO

> 對應計劃：`docs/plan/unfinish/A16_與B合流與Demo演練.md`。本篇不改 `showme/**`、`tests/**`、`overlay/**`。
> 前提：A01–A15 全綠（169 passed）；B 的真 overlay 已進 repo；示範站 finefoods-antd 已在 `:3000` 跑著。

## 1. 實作邏輯

A16 分兩類：**機器能驗的**（接縫對照、示範站、headed Chrome、完整流程）由 agent 直接在真 overlay + 真示範站上跑一次並記錄實測數字；**需要 Qoder IDE 與真人的**（Request Timeout、allow list、對話彩排）留給使用者，在 checklist 標「待使用者」。

```text
Step 1-1 pytest -m browser ─┐
Step 1-2 接縫 14 項 ────────┼── agent 用 PlaywrightBrowser + 真 overlay 打 :3000，逐項印證據
Step 1-3 CSP 症狀 ──────────┤   （收 console 紅字）
Step 2/3 示範站 + dev_open ─┤
Step 5 彩排（程式化）───────┘   ShowMeApp 走 start → click → input → stuck → timeout → end → 再 end
                                 ⚠ 不按 Save/Create 送出表單（finefoods 的 API 是外部服務，不留資料）
Step 6/7 checklist、設計決定表 → 能勾的勾、數字填進去、Qoder 相關標「待使用者」
Step 8 搬 A00/A16 到 finish/ → 主控 commit
```

## 2. 步驟
- [x] Step 1-1 `uv run pytest -m browser -q` 全綠
- [x] Step 1-2 接縫 14 項在 :3000 逐項確認並記錄
- [x] Step 1-3 console 無 CSP 紅字
- [x] Step 2/3 示範站 `:3000` 可開、`dev_open.py` headed 彈窗
- [x] Step 5 程式化彩排一次（含 input、stuck、timeout、end、再 end → session_not_found）；填六個實測數字（Qoder 兩格待使用者）
- [x] Step 6 checklist 打勾（Qoder 段待使用者）
- [x] Step 8 A00、A16 搬到 `finish/`；REP；commit

## 3. 完成條件（2026-08-29 達成：接縫 14/14、彩排全程照規格；Qoder 相關項待使用者；彩排後補了 snapshot 空清單重拍）
- A16 §8 驗收清單：機器可驗項全勾；Qoder 相關項明確標「待使用者」
- `docs/plan/unfinish/` 空
