# 2026-08-29 A11～A15 實作 TODO

> 對應計劃：`docs/plan/unfinish/A11` ～ `A15`（A 工程師 / Python 側）。
> 觸發：`docs/plan/dev-prompts/phase0829-1.md`。
> 檔名對照：prompt 寫的 `A13_show_step完成.md`／`A14_show_step偏離腳本.md` 不存在；實際檔案是 `A13_end_tutorial.md`／`A14_MCP契約測試與stdio.md`。`off_script`（偏離腳本）是規格 Non-Goal，不做。
> 前提：B 的 `overlay/overlay.js` 已 pull 進來（真 overlay，517 行），A05 的瀏覽器測試已在它上面跑綠。

---

## 0. 計劃文件現況檢查（做之前要套的差異）

A11–A15 寫於 A01–A10 實作前，已知要套的差異（由執行 agent 邊做邊改文件那幾行）：

| 差異 | 來源 | 影響 |
|---|---|---|
| tool 方法回傳註記 `-> dict` → `-> dict[str, object]` | A01 實測：裸 dict 讓 `structured_content` 變 `None` | A11–A13 的 `show_step`/`end_tutorial` 簽名 |
| `tests/test_fakes.py` 末尾 parametrize 只剩 `show_step`、`end_tutorial` 兩行 | A08/A10 已各刪一行 | A12 刪 `show_step`、A13 刪 `end_tutorial`（刪到空就整個測試拿掉） |
| 全套件「N passed」累積數字 | 目前 119 passed, 1 skipped | 改成實際數字 |
| `structured_content` 已是裸 dict | A07 起用 `dict[str, object]` | A14 的 `payload()` helper 兩種都能接，不用改邏輯 |
| B 的 emit 多帶 `signal` 鍵 | overlay.src.js L193 | Python `_on_emit` 只看 `kind`，多的鍵忽略 |

---

## 1. 實作邏輯

```text
A10 已完成（start_tutorial / inspect_page 可用；show_step / end_tutorial 仍佔位）
 │
 ├──▶ 階段 1-P：A11 show_step 六道前置檢查 ──▶ A12 阻塞等待/timeout/狀態機 ──▶ A13 end_tutorial
 │              （三篇都改 app.py 同一個檔 → 串行；A12 順便打開 A09 的 OQ2 skip、更新 A11 的 4 條佔位斷言）
 │                                                                                          ┐
 └──▶ 階段 1-Q：A14 MCP 契約測試（in-memory Client）+ stdio 手動驗證 + Qoder 設定           │ 平行
                （只碰 tests/test_mcp_contract.py、conftest 的 mcp_client fixture）        ┘
                       │
                       ▼
               階段 2：A15 真瀏覽器端到端（假 overlay）
                       + 加碼：一條「真 overlay」e2e（start → show_step click → 模擬人點按鈕 → URL 變 → 真 overlay emit → show_step 回 step_done → end 出 banner）
```

- P 與 Q 改的檔案不重疊，可平行；A15 依賴 A12/A13 的 show_step/end_tutorial，所以在後。
- 平行 agent **不 commit**，主控在階段結束統一 commit、搬計劃到 `finish/`、確認 REP。

---

## 2. 步驟

### 階段 1-P：A11 → A12 → A13（1 個 agent）
- [ ] A11 `show_step` 前置六關（session_not_found / show_step_in_progress / max_steps_exceeded / expect_text_required / uid_not_in_snapshot+新鮮 page / timeout_s 正規化），`tests/test_tool_show_step_checks.py`
- [ ] A12 畫＋阻塞（Future / wait_for / shield）、timeout 判定、`cancelled` 分支、`tests/test_tool_show_step_wait.py`；更新 A11 四條佔位斷言；打開 A09 OQ2 skip；刪 test_fakes 的 `show_step` 行
- [ ] A13 `end_tutorial`（clear → done(固定文案) → 刪 Session），`tests/test_tool_end.py`；刪 test_fakes 的 `end_tutorial` 行；`grep not_implemented showme/` 應為空

### 階段 1-Q：A14（1 個 agent，與 P 平行）
- [ ] conftest 加 `mcp_client` fixture；`tests/test_mcp_contract.py`（四個 tool、無 wait_for_user、error 不變 is_error、show_step schema、instructions）
- [ ] stdio 手動驗證（subprocess 送 JSON-RPC initialize）
- [ ] Qoder 設定 JSON、allow list 寫進文件；Request Timeout 量測需要 Qoder IDE，留給使用者手動

### 階段 2：A15 + 真 overlay e2e（1 個 agent）
- [ ] `tests/test_e2e_fake_overlay.py`（假 overlay：start → show_step → 手動 emit → timeout → end → 再 end）
- [ ] `tests/test_e2e_real_overlay.py`（真 overlay：模擬人點擊，靠 overlay 自己 emit）
- [ ] 全套件綠；commit；A11–A15 搬到 `finish/`；REP

---

## 3. 完成條件

- `uv run pytest` 全綠（含 browser）；`showme/app.py` 不再有 `not_implemented`
- A11～A15 驗收清單逐條打勾；計劃搬到 `docs/plan/finish/`；REP 在 `docs/plan/report/`
