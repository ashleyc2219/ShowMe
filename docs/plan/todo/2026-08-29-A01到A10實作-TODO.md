# 2026-08-29 A01～A10 實作 TODO

> 對應計劃：`docs/plan/unfinish/A01` ～ `A10`（A 工程師 / Python 側）。
> 觸發：`docs/plan/dev-prompts/phase0829.md`。
> 原則：照計劃做、TDD（先紅再綠）、不鑽牛角尖、能平行就平行。

---

## 0. 計劃文件現況檢查

A01～A10 是 2026-08-29 當天依「目前 repo 現況」寫的（骨架 `showme/server.py` 四個 tool 回 `not_implemented`、`tests/` 空、沒有 `.venv`／`uv.lock`），與現況一致，**不需要事前更新**。
執行中若發現某一步與實際不符（指令、輸出、API），由執行的 agent 以「能跑通」為準做最小修正，並同步改計劃文件那幾行。

---

## 1. 實作邏輯（為什麼這樣分階段）

```text
A01 環境（必須最先）
 │
 ├──▶ 階段 2-X：A02 Session ──▶ A03 純函數        （純 Python，不開瀏覽器）
 │                                                  ┐
 └──▶ 階段 2-Y：A04 開頁 ──▶ A05 注入+emit ──▶ A06 JS 呼叫  │ 兩條線平行
                                                    ┘
                       │
                       ▼
               階段 3：A07 FakeBrowser + App 骨架（收斂點：同時要 A03 的 rules、A06 的 BrowserLike）
                       │
                       ▼
               階段 4：A08 start_tutorial ──▶ A09 覆蓋場次 ──▶ A10 inspect_page
                       （三篇都改 app.py，A10 的測試用到 A08 的 started fixture → 串行）
```

- 階段 2 兩條線改的檔案完全不重疊（`session.py`/`rules.py` vs `browser.py`/`conftest.static_server`/`fixtures/`），可安全平行。
- 平行的 agent **不 commit**，由主控在每階段結束統一 commit（避免兩個 agent 同時動 git index）。
- 每階段結束：`uv run pytest -m "not browser"` 全綠 → commit → 把完成的計劃從 `unfinish/` 搬到 `finish/` → 寫 REP。

---

## 2. 步驟

### 階段 1：A01 環境建置（1 個 agent）
- [x] `uv python pin 3.12`、`pyproject.toml` 加 dev group + pytest 設定、`uv sync`、`uv run playwright install chromium`
- [x] `tests/conftest.py`（`anyio_backend`）、`tests/test_smoke.py`（四個 tool、無 `wait_for_user`）
- [x] 實測 tool 回傳 dict 時 `structured_content` 形狀（給 A07/A14 用）
- [x] `uv run showme` 能啟動
- [x] commit `chore:`；A01 搬到 `finish/`；REP

### 階段 2：平行兩條線（2 個 agent）
- [x] X：A02 `showme/session.py` + `tests/test_session.py` → A03 `showme/rules.py` + `tests/test_rules.py`
- [x] Y：A04 `showme/browser.py`（launch/open/close）+ `static_server` + `dashboard.html` + `scripts/dev_open.py` → A05 `add_init_script` + `expose_function` → A06 `snapshot/show/clear/done` + `fake_overlay.js` + 兩個 HTML
- [x] 兩邊都過 → commit（每篇一個 `feat:`/`test:`）；A02–A06 搬到 `finish/`；REP

### 階段 3：A07（1 個 agent）
- [x] `tests/fakes.py` FakeBrowser、`showme/app.py` 骨架、`showme/server.py` 薄殼、conftest fixture
- [x] A01 的 smoke test 仍綠；commit；A07 搬到 `finish/`；REP

### 階段 4：A08 → A09 → A10（1 個 agent，串行）
- [x] A08 `start_tutorial` 新建 + `navigation_failed`
- [x] A09 覆蓋場次 + 瀏覽器死掉重建 + OQ2 測試（skip 到 A12）
- [x] A10 `inspect_page`
- [x] commit；A08–A10 搬到 `finish/`；REP

---

## 3. 完成條件（2026-08-29 全部達成：全套件 119 passed, 1 skipped；skip 為 A09 OQ2，待 A12 打開）

- `uv run pytest -m "not browser"` 全綠；`uv run pytest -m browser` 全綠（A04–A06 的瀏覽器測試）
- A01～A10 每篇「驗收清單」逐條打勾
- `git status` 乾淨、計劃文件已在 `docs/plan/finish/`、四份 REP 在 `docs/plan/report/`
